---
- hosts: localhost
  gather_facts: false
#  user: dev
  become: true
  tasks:
  - name: Copy file
    copy:
      src: {{ playbook_dir}}/hello.txt
      dest: /home/dev/
      owner: dev
      group: dev
      mode: '0644' 
      tags: upload
  #- name: Make Work Directory
  #  file:
  #    path: /home/dev/hello
  #    owner: dev
  #    group: dev
  #    mode: 0755
