---
- hosts: "127.0.0.1"
  become: true
  tasks:
    - name: ローカルのファイルをリモートにコピー
      copy: 
        src=hello.txt
        dest=/home/dev
        owner=root
        group=root
        mode=0700
      tags: upload
 
#    - name: Apacheをインストール
#      yum: name=httpd state=latest
